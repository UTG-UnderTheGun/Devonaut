import '../globals.css';

export const metadata = {
  title: 'Assignment Feedback - Devonaut',
  description: 'View feedback for your submitted assignments',
}

export default function FeedbackLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
} 